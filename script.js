let openSquares = [1, 2, 3, 4, 5, 6, 7, 8, 9];

const createPlayer = (name, letter) => {
    return { name, letter };
};

const jeff = createPlayer('Jeff', 'O');
const mary = createPlayer('Mary', 'X');

